import React from "react";

export default function Patients() {
  return (
    <div className="page container">
      <h2>Patient Records</h2>
      <p className="muted">Patient list will be fetched from the backend (doctor/admin).</p>
      <div className="card">No data (stub).</div>
    </div>
  );
}
